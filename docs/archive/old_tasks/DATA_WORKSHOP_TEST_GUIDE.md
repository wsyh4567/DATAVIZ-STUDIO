# 数据工坊实时预览 - 测试指南

## 访问页面

应用已启动，访问：**http://127.0.0.1:8050/workshop**

## 功能测试清单

### 1. 加载数据 ✅
- [ ] 点击"加载示例数据"按钮
- [ ] 验证数据表格显示8行5列数据
- [ ] 验证统计信息显示"8 行 × 5 列"

### 2. 执行操作 ✅
- [ ] 点击"筛选"按钮
  - 应该筛选出 city == 'NYC' 的数据（3行）
  - 步骤面板应显示"步骤 1"
  - 数据预览应更新
  
- [ ] 点击"类型转换"按钮
  - 应该将 age 列从字符串转为数值
  - 步骤面板应显示"步骤 2"
  
- [ ] 点击"排序"按钮
  - 应该按 salary 降序排序
  - 步骤面板应显示"步骤 3"

### 3. 撤销重做 ✅
- [ ] 点击"撤销"按钮
  - 应该回到上一个状态
  - 撤销按钮应该可用（如果有历史）
  
- [ ] 点击"重做"按钮
  - 应该前进到下一个状态
  - 重做按钮应该可用（如果有未来状态）

### 4. 步骤管理 ✅
- [ ] 查看步骤列表
  - 每个步骤应显示操作描述
  - 显示影响的行列数
  
- [ ] 点击"清空所有步骤"
  - 应该清空所有操作
  - 数据应恢复到原始状态

### 5. 代码生成 ✅
- [ ] 点击"查看代码"按钮
  - 应该打开代码预览模态框
  - 显示生成的Python代码
  - 包含导入语句和数据加载
  - 包含所有操作步骤的代码
  
- [ ] 点击"复制代码"按钮
  - 应该复制代码到剪贴板
  - 显示成功提示（Toast通知）
  
- [ ] 点击"下载 .py"按钮
  - 应该下载Python文件

## 示例操作流程

### 完整测试流程

1. **加载数据**
   ```
   点击"加载示例数据" → 看到8行数据
   ```

2. **筛选数据**
   ```
   点击"筛选" → 只显示NYC的3行数据
   ```

3. **类型转换**
   ```
   点击"类型转换" → age列从字符串变为数值
   ```

4. **排序**
   ```
   点击"排序" → 按salary降序排列
   ```

5. **查看代码**
   ```
   点击"查看代码" → 看到完整的Python代码
   ```

6. **撤销操作**
   ```
   点击"撤销" → 回到排序前的状态
   ```

7. **重做操作**
   ```
   点击"重做" → 恢复排序
   ```

## 预期生成的代码

执行上述操作后，生成的代码应该类似：

```python
import pandas as pd
import numpy as np
from datetime import datetime

# 加载数据
df = pd.read_csv('data.csv')
print(f"数据形状: {df.shape}")

# 步骤1: filter
df = df[df['city'] == 'NYC']
print(f'筛选后行数: {len(df)}')

# 步骤2: type_conversion
df['age'] = pd.to_numeric(df['age'], errors='coerce')
print(f'列 age 已转换为数值型')

# 步骤3: sort
df = df.sort_values(by='salary', ascending=False)
print(f'已按 salary 降序排序')
```

## 已知功能

### ✅ 已实现
- 加载示例数据
- 基础操作（筛选、类型转换、排序）
- 实时预览更新
- 步骤管理面板
- 撤销重做功能
- 代码生成和预览
- 代码复制（客户端JS）
- 代码下载

### 🔄 演示模式
当前版本使用演示操作：
- 筛选：固定筛选 city == 'NYC'
- 类型转换：固定转换 age 为数值
- 排序：固定按 salary 降序

### ⏳ 待实现
- 操作参数配置界面
- 更多操作类型（删除列、重命名、填充缺失值等）
- 步骤拖拽重排序
- 键盘快捷键（Ctrl+Z/Ctrl+Y）
- 列头右键菜单
- 筛选面板交互

## 故障排除

### 问题：页面显示旧版本
**解决方案**：
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 硬刷新页面（Ctrl+F5）
3. 重启应用

### 问题：操作按钮无响应
**解决方案**：
1. 确保已加载示例数据
2. 检查浏览器控制台是否有错误
3. 查看终端日志

### 问题：代码复制不工作
**解决方案**：
1. 确保浏览器支持 Clipboard API
2. 检查 `assets/js/code_copy.js` 是否加载
3. 手动选择代码并复制

## 性能指标

### 预期性能
- 加载数据：< 100ms
- 执行操作：< 200ms
- 代码生成：< 50ms
- 撤销重做：< 100ms

### 测试数据
- 行数：8行
- 列数：5列
- 数据大小：< 1KB

## 下一步开发

1. **操作配置界面**
   - 筛选条件配置
   - 类型转换选项
   - 排序列选择

2. **更多操作**
   - 删除列
   - 重命名列
   - 填充缺失值
   - 去重
   - 列拆分/合并

3. **高级功能**
   - 步骤拖拽排序
   - 键盘快捷键
   - 列头菜单
   - 筛选面板

## 反馈

如果发现任何问题或有改进建议，请记录：
- 问题描述
- 重现步骤
- 预期行为
- 实际行为
- 浏览器和版本

---

**测试日期**: 2024年
**版本**: v0.1.0 (演示版)
**状态**: 核心功能可用，演示模式

# 🚀 DataViz Studio（独立版）— AI 项目构建提示词

> **独立本地 Web 应用**：Dash + Plotly 构建，Power BI / Tableau / FineBI 级零代码数据分析平台

---

## Prompt 0：项目总纲（每次对话开头必须提供）

```
你是一个资深全栈 Python 开发者，精通 Dash、Plotly、pandas 和现代 Web 应用设计。
你正在构建 DataViz Studio —— 一个免费开源的零代码数据分析可视化平台。

产品定位：
- 对标 Power BI Desktop / Tableau / FineBI
- 免费、开源、基于 Python 生态
- 本地运行（localhost），无需云服务
- 面向不会写代码的数据分析人员、运营、产品经理、学生、科研人员
- 填补市场空白：目前没有一个免费开源的 Power BI 级别产品

启动方式：
  $ dataviz-studio              # 命令行启动
  → 自动打开浏览器 localhost:8050
  → 显示欢迎页，拖入数据即可开始分析

技术栈：
- 框架：Dash (Plotly) —— 基于 Flask + React，纯 Python 开发
- 图表：Plotly.js（交互式图表，原生 Dash 集成）
- 数据表格：Dash AG Grid（高性能表格）
- 数据处理：pandas, numpy, scipy, scikit-learn
- 样式：Dash Bootstrap Components + 自定义 CSS
- 部署：本地运行为主，也可部署到服务器供团队使用
- 打包：PyInstaller 打包为单个 .exe（可选）

设计原则：
- 零代码：用户全程通过 GUI 操作，永远不需要写一行代码
- 即时反馈：任何操作 < 1 秒看到结果
- 渐进披露：常用功能一键触达，高级功能按需展开
- 专业美观：不能看起来像个 Demo，要像商业产品
- 数据安全：所有数据留在本地，不上传任何服务器
```

---

## Prompt 1：应用外壳与导航

```
构建 DataViz Studio 的应用外壳，这是整个产品的入口和框架。

整体布局（对标 Power BI Desktop）：
┌──────────────────────────────────────────────────────┐
│  🧪 DataViz Studio          [🔔] [⚙️设置] [❓帮助]   │  ← 顶栏
├────┬─────────────────────────────────────────────────┤
│ 📊 │                                                 │
│ 📁 │                                                 │
│ 🧹 │          主工作区（根据左侧导航切换）              │
│ 📈 │                                                 │
│ 🧮 │                                                 │
│ 📋 │                                                 │
│    │                                                 │
│ ⚡ │                                                 │
├────┴─────────────────────────────────────────────────┤
│  状态栏：当前数据集 | 行×列 | 内存 | 操作日志          │
└──────────────────────────────────────────────────────┘

左侧导航栏（图标 + 收起/展开）：
├── 📊 数据画布（概览与预览）
├── 📁 数据中心（加载与管理）
├── 🧹 数据工坊（清洗与转换）
├── 📈 图表工作室（核心：拖拽式创建图表）
├── 🧮 统计实验室（统计分析与假设检验）
├── 📋 仪表盘（多图组合看板）
└── ⚡ 高级工具（时间序列/对比/特征工程...）

页面路由：
- 使用 Dash Pages 或 dcc.Location 实现 SPA 路由
- /welcome → 欢迎页（最近项目 + 快速开始）
- /data → 数据中心
- /canvas → 数据画布
- /workshop → 数据工坊
- /charts → 图表工作室
- /stats → 统计实验室
- /dashboard → 仪表盘
- /advanced → 高级工具

欢迎页设计：
- 中央大区域支持拖拽文件上传
- "最近打开的项目"列表
- "示例数据集"快速体验入口（鸢尾花、泰坦尼克、销售数据...）
- 产品 Logo + 简介动画

全局状态管理：
- 使用 dcc.Store 存储全局数据状态
- 当前活跃 DataFrame 的引用
- 操作历史栈（支持全局撤销/重做）
- 用户偏好设置（主题、语言等）
```

---

## Prompt 2：数据中心（Data Hub）

```
构建数据中心模块 —— 数据的入口和管理中枢。

数据导入面板（主区域上半部分）：
┌─────────────────────────────────────────┐
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│  │ CSV │ │Excel│ │JSON │ │数据库│ ...   │  ← 数据源卡片
│  └─────┘ └─────┘ └─────┘ └─────┘       │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │                                   │  │
│  │    📂 拖拽文件到此处 或 点击选择    │  │  ← 上传区
│  │                                   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

支持的数据源：
- 文件：CSV / TSV / Excel(.xlsx/.xls) / JSON / Parquet / Feather / STATA / SAS / SPSS
  → 自动检测编码（UTF-8/GBK/Latin1...）
  → 智能推断分隔符、表头行、数据类型
  → Excel 支持选择 Sheet
- 数据库：SQLite / MySQL / PostgreSQL / SQL Server
  → 可视化连接表单（主机/端口/用户名/密码）
  → 内嵌 SQL 编辑器（语法高亮 + 自动补全）
  → 表结构预览 + 一键拉取
- URL：输入链接直接下载远程文件
- 粘贴板：从 Excel/网页复制表格，一键粘贴导入
- 示例数据集：seaborn / sklearn 经典数据集一键加载

数据管理面板（主区域下半部分）：
- 已加载数据集列表（卡片式展示）
  │ 📊 sales_2024.csv
  │ 12,450 行 × 8 列 | 2.3 MB | 已加载 3 分钟前
  │ [设为活跃] [重命名] [复制] [删除]
- 数据集操作：
  - 合并两个数据集（可视化 merge 配置：选择 key 列、合并方式）
  - 纵向拼接（concat）
  - 从已有数据集创建子集（筛选条件 → 另存为新数据集）
```

---

## Prompt 3：数据画布（Data Canvas）

```
构建数据画布模块 —— 像 Excel 一样直观地浏览和理解数据。

数据表格（占据主区域，这是用户看到数据的第一个视图）：
- 基于 Dash AG Grid 的高性能表格
- 支持 100 万行级别虚拟滚动
- 功能：
  ├── 列排序（点击列头）
  ├── 列筛选（列头下拉筛选器，支持条件/范围/搜索）
  ├── 列拖拽调序
  ├── 列宽拖拽调整
  ├── 冻结列 / 冻结行
  ├── 条件格式（数值列渐变色、空值标红、负值标红）
  ├── 单元格点击选中 → 底栏显示该单元格详情
  └── 行选择（勾选框，可批量操作选中行）

数据概览仪表盘（表格上方或可切换的子视图）：
┌────────┬────────┬────────┬────────┬────────┐
│ 总行数  │ 总列数  │ 缺失值  │ 重复行  │ 内存    │
│ 12,450 │ 8      │ 234    │ 15     │ 2.3 MB │
│        │        │ (1.9%) │ (0.1%) │        │
└────────┴────────┴────────┴────────┴────────┘

列级摘要（点击列头或侧边面板浏览）：
- 数值列：迷你直方图 + 均值/中位数/标准差/最小/最大/四分位
- 分类列：频率条形图 + 唯一值数 + 最频繁值 + 类别列表
- 日期列：时间范围 + 迷你时间线 + 粒度（天/月/年）
- 布尔列：True/False 占比环形图

数据质量评分：
- 综合评分（0-100）：考虑缺失率、重复率、类型一致性、异常值比例
- 每列独立评分
- 红/黄/绿色标注问题严重程度
- 点击问题 → 直接跳转到数据工坊的对应清洗操作

缺失值地图：
- 矩阵式可视化（列×行采样），黑色=缺失，白色=有值
- 快速识别缺失模式（随机缺失 / 系统性缺失 / 块状缺失）
```

---

## Prompt 4：数据工坊（Data Workshop）

```
构建数据工坊 —— 全程 GUI 的数据清洗和转换中心。

界面布局：
┌────────────────┬────────────────────┬──────────────┐
│  操作菜单       │   数据预览          │ 操作流水线    │
│  (分类列表)     │   (实时显示效果)    │ (操作历史)    │
│                │                    │              │
│ ▸ 列操作       │                    │ ① 删除列 age │
│ ▸ 缺失值       │   > 应用前可预览 <  │ ② 填充缺失值  │
│ ▸ 类型转换      │   > 变化高亮显示 <  │ ③ 类型转换    │
│ ▸ 筛选排序      │                    │ ④ ...       │
│ ▸ 文本处理      │                    │              │
│ ▸ 数值处理      │                    │ [撤销] [重做] │
│ ▸ 计算列       │                    │ [导出脚本]    │
└────────────────┴────────────────────┴──────────────┘

操作分类（每个操作都是一个 GUI 表单，绝不需要写代码）：

列操作：
  - 删除列：勾选多列 → 点"删除"
  - 重命名：点击列名直接编辑
  - 调序：拖拽排序
  - 拆分列：选列 → 选分隔符 → 预览 → 确认
  - 合并列：选多列 → 设分隔符 → 新列名 → 确认

缺失值处理：
  - 可视化面板：哪些列有多少缺失（条形图排序展示）
  - 对每列独立选择策略：
    ├── 删除整行
    ├── 填充：均值/中位数/众数/固定值
    ├── 前向填充 / 后向填充
    └── KNN 智能插值
  - 批量操作：对所有数值列统一填充均值

数据类型转换：
  - 当前类型 → 目标类型 下拉选择
  - 转换预览（成功/失败数量提示）
  - 智能建议（"此列看起来是日期，建议转为 datetime"）

筛选：
  - 可视化条件构建器
  - 条件卡片拖拽组合（AND / OR）
  - 每个条件：[列名 ▼] [操作符 ▼] [值输入]
  - 实时显示筛选后剩余行数

排序与去重：
  - 多列排序（拖拽优先级）
  - 去重：选择判断列 + 保留策略（首个/末个）

文本处理（字符串列）：
  - 去空格 / 转大写 / 转小写 / 首字母大写
  - 查找替换（支持正则）
  - 提取模式（正则提取 + 可视化说明）
  - 文本分列

数值处理：
  - 分箱/离散化（等宽/等频/自定义边界）
  - 标准化 / 归一化
  - 取对数 / 取绝对值
  - 四舍五入

新增计算列：
  - 常用模板：同比增长、环比增长、累计求和、排名、占比
  - 简单公式构建器（选列 → 选运算 → 设置参数）
  - 高级：pandas 表达式输入（带语法高亮和自动补全）

操作流水线（核心特性）：
  - 每步操作记录为卡片，显示操作类型 + 影响范围
  - 可拖拽排序、禁用/启用单步、删除单步
  - 一键导出为 Python 脚本（可复制到 Jupyter 中执行）
  - 流水线可保存为"模板"，下次加载新数据直接应用
```

---

## Prompt 5：图表工作室（Chart Studio）—— 核心模块

```
这是 DataViz Studio 最核心的模块，要提供 Tableau / Power BI 级别的
拖拽式图表创建体验。

界面布局：
┌──────────┬─────────────────────────┬─────────────┐
│ 字段面板  │                         │ 配置面板     │
│          │                         │             │
│ 度量(数值)│                         │ 图表类型     │
│ ├ revenue│     图 表 画 布           │ [网格选择]   │
│ ├ quantity│    （实时渲染）           │             │
│ ├ profit │                         │ 样式设置     │
│          │                         │ ├ 主题      │
│ 维度(分类)│                         │ ├ 配色      │
│ ├ city   │                         │ ├ 字体      │
│ ├ product│                         │ ├ 标题      │
│ ├ date   │                         │ └ 图例      │
│          │                         │             │
│──────────│                         │ 导出        │
│ 拖放区域  │                         │ [PNG] [SVG] │
│ X轴: [ ] │                         │ [PDF] [HTML]│
│ Y轴: [ ] │                         │             │
│ 颜色: [ ] │                         │             │
│ 大小: [ ] │                         │             │
│ 分面: [ ] │                         │             │
└──────────┴─────────────────────────┴─────────────┘

字段面板：
- 自动将列分为"度量"（数值）和"维度"（分类/日期）
- 字段旁显示类型图标（#数值 Abc文本 📅日期）
- 支持拖拽到拖放区域 或 下拉选择（两种交互方式）
- 拖入字段后自动推断合适的聚合方式
  度量默认 SUM，维度默认 GROUP BY

图表类型（图标网格选择器，分场景组织）：
  比较：柱状图 | 分组柱状图 | 堆叠柱状图 | 条形图 | 雷达图
  趋势：折线图 | 面积图 | 堆叠面积图
  分布：直方图 | 箱线图 | 小提琴图 | 密度图 | 蜂群图
  关系：散点图 | 气泡图 | 热力图 | 相关矩阵
  占比：饼图 | 环形图 | 旭日图 | 矩形树图
  高级：漏斗图 | 瀑布图 | 桑基图 | 平行坐标 | K线图
  地图：散点地图 | 填充地图（如有地理字段）

智能推荐：
- 根据拖入字段类型自动推荐最佳图表
  1 维度 + 1 度量 → 推荐柱状图
  日期 + 度量 → 推荐折线图
  2 度量 → 推荐散点图
  1 维度(少量类别) + 1 度量 → 推荐饼图
- 不兼容的图表灰显 + 提示原因

图表画布：
- 使用 plotly 渲染，保留所有原生交互（缩放/平移/悬停提示）
- 实时更新：字段/配置变化后 < 500ms 刷新图表
- 支持在图表上添加：
  ├── 趋势线（线性/多项式拟合）
  ├── 参考线（均值线/中位数线/自定义值）
  ├── 数据标签（显示数值）
  └── 注释（文本箭头指向特定数据点）

配置面板：
- 主题预设（12+ 套）：
  商务蓝 | 科技暗 | 自然绿 | 学术灰 | 极简白
  热情红 | 优雅紫 | 清新青 | 复古棕 | 渐变系列...
- 调色板：D3/ColorBrewer 经典调色板 + 自定义
- 标题/副标题编辑
- 轴：标签、范围、刻度、对数轴开关
- 图例：位置/方向/开关
- 网格线/背景色
- 动画开关（有时间维度时支持播放动画）

图表管理：
- 每个创建的图表可命名保存
- 已保存图表列表（缩略图预览）
- 图表可复制 / 编辑 / 删除
- 保存的图表可在"仪表盘"模块中使用

导出：
- PNG / SVG / PDF（高清静态）
- HTML（保留 Plotly 交互功能的独立页面）
- 复制到剪贴板
```

---

## Prompt 6：仪表盘构建器（Dashboard Builder）

```
构建仪表盘模块 —— 把多个图表组合成一个交互式数据看板。
这是 Power BI 最核心的功能之一。

界面：
┌──────────────────────────────────────────────────┐
│ [添加图表] [添加指标卡] [添加筛选器] [添加文本]    │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌──────────┐  ┌──────────────────┐              │
│  │ 总营收    │  │                  │              │
│  │ ¥1,234万  │  │  月度趋势折线图   │              │
│  │ ↑12.3%   │  │                  │              │
│  └──────────┘  └──────────────────┘              │
│  ┌─────────────────┐  ┌─────────────────┐        │
│  │                  │  │                 │        │
│  │  各城市柱状图     │  │  品类饼图        │        │
│  │                  │  │                 │        │
│  └─────────────────┘  └─────────────────┘        │
│                                                  │
└──────────────────────────────────────────────────┘

核心功能：
- 画布式布局：组件自由拖拽定位 + 拖拽调整大小
- 网格对齐吸附（防止错位）

可添加的组件：
├── 图表：从"图表工作室"已保存的图表中选择，或就地新建
├── 指标卡（KPI Card）：
│   大数字 + 标题 + 趋势箭头（↑↓）+ 对比值
│   配置：选度量字段 → 选聚合方式 → 选对比基准
├── 筛选器控件：
│   下拉选择 / 日期范围选择器 / 滑块 / 搜索框
│   一个筛选器可联动控制看板上所有图表
├── 文本框：Markdown 格式，可写标题/说明
└── 分隔线/装饰元素

交叉筛选（Power BI 核心体验）：
- 点击柱状图某根柱子 → 其他图表自动筛选到对应数据
- 框选散点图区域 → 其他图表联动更新
- 筛选器控件 → 控制所有图表
- 这是优先级最高的交互特性，必须实现

仪表盘管理：
- 多个仪表盘页面（标签页切换）
- 保存 / 加载
- 导出为独立 HTML 页面（可分享给别人打开）
- 导出为 PDF
- 全屏演示模式
```

---

## Prompt 7：统计实验室（Statistics Lab）

```
构建统计实验室 —— 面向非统计专业用户的引导式分析工具。

设计理念：用问答引导代替专业术语。
用户不需要知道"t检验"是什么，只需要回答"你想比较什么"。

描述性统计：
- 选择列 → 美化卡片展示（不是 describe() 的原始表格）
- 每个指标配可视化：均值在分布图上标注位置
- 自然语言摘要："该列呈右偏分布，均值(45.2)高于中位数(38.0）"

相关性分析面板：
- 交互式热力图（鼠标悬停显示具体相关系数）
- 点击任意交叉格 → 弹出散点图 + 拟合线 + R² 值
- 一键排序：按相关强度排列变量对
- 自动标注：强相关/强负相关/微弱相关

分组聚合分析器：
- Step 1: 选分组字段（可多级分组）
- Step 2: 选度量字段 + 聚合函数（下拉选择）
- Step 3: 即时显示结果表格 + 自动推荐可视化
- 支持导出结果为新的 DataFrame

数据透视表：
- 拖拽字段到 行/列/值 区域（完全模仿 Excel 透视表）
- 实时预览
- 支持多层行/列
- 汇总行/列

假设检验向导（引导式）：
┌─ 你想做什么？──────────────────────────────────┐
│                                               │
│  ○ 比较两组数据是否有显著差异                    │
│  ○ 比较多组（3组以上）数据是否有差异             │
│  ○ 检验两个分类变量是否相关                      │
│  ○ 检验数据是否符合正态分布                      │
│  ○ 检验数据趋势是否显著                         │
│                                               │
│  选择后 → 自动引导选择对应的列                    │
│        → 自动运行对应检验                        │
│        → 用大白话解释结果                        │
│                                               │
│  示例结果：                                     │
│  "两组数据存在显著差异（p=0.003）。              │
│   这意味着差异不太可能是巧合，可信度 99.7%。"     │
└───────────────────────────────────────────────┘

异常值检测：
- 方法选择：IQR / Z-Score / Isolation Forest
- 图表上标注异常点（红色高亮）
- 异常值列表 + 统计信息
- 一键处理：标记/删除/替换为边界值
```

---

## Prompt 8：高级与独创功能

```
以下功能超越 Power BI / Tableau / 现有开源工具，
是 DataViz Studio 的差异化竞争力。
方向性描述，请根据技术可行性自由发挥最佳实现方案。

1. 数据故事（Data Storytelling）
   将分析过程编排成一个可滚动叙事页面：
   - 图表 + 文字注释 + 过渡动画
   - 类似 Scrollytelling 的体验
   - 可导出为独立 HTML / PDF 报告
   - 用例：给老板汇报不发 PPT，发一个数据故事链接

2. 智能数据对比
   选两个数据集或两个子集，自动生成对比分析：
   - 各列统计指标并排对比（变化高亮）
   - 分布对比图（重叠直方图/密度图）
   - 差异热力图
   - 用例：本月 vs 上月、A/B 测试、清洗前后对比

3. 时间序列专区
   检测到日期列时自动推荐进入：
   - 趋势分解（趋势 + 季节性 + 残差 可视化）
   - 移动平均 / 指数平滑
   - 自相关图 / 偏自相关图
   - 简易预测（扩展历史趋势，带置信区间）
   - 多序列对比面板

4. 特征工程助手
   面向准备做机器学习的用户：
   - 自动检测建议：独热编码、标准化、Log变换...
   - 特征重要性快速评估（随机森林）
   - 特征交叉/组合建议
   - 一键导出 scikit-learn Pipeline 代码

5. 自然语言查询栏
   顶部搜索框输入自然语言：
   "按城市分组统计总销售额排名前10"
   → 自动转为 pandas 操作 → 显示结果 + 生成的代码
   可接入 LLM API 或做基于规则的简单版

6. 数据连接器扩展
   - REST API 拉取向导（URL + 参数 + 认证 + JSON路径设置）
   - 定时刷新数据（本地调度）
   - 多数据源关联（可视化 join 配置）

7. 项目系统
   - 分析状态保存为 .dvs 项目文件（JSON 格式）
   - 打开项目 → 恢复所有数据、图表、仪表盘、操作历史
   - 导出为 Jupyter Notebook（所有操作转化为代码 Cell）

8. 多语言支持
   - 中文/英文 UI 切换
   - 国际化框架（i18n），方便社区贡献其他语言
```

---

## Prompt 9：设计系统与视觉规范

```
为 DataViz Studio 设计一套对标商业产品的视觉系统。

设计目标：第一眼看到时要让人觉得这是一个商业级产品，
不是学生作业或 Demo。

配色方案（暗色主题，默认）：
  --bg-primary:    #0F1117    (最深背景)
  --bg-secondary:  #1B1D2A    (面板背景)
  --bg-tertiary:   #262940    (卡片/输入框背景)
  --bg-hover:      #2E3148    (悬停状态)
  --accent:        #6366F1    (主强调色 - Indigo)
  --accent-hover:  #818CF8    (强调色悬停)
  --text-primary:  #F1F5F9    (主文字)
  --text-secondary:#94A3B8    (次文字)
  --text-muted:    #64748B    (最淡文字)
  --success:       #10B981    (成功/正增长)
  --warning:       #F59E0B    (警告)
  --error:         #EF4444    (错误/负增长)
  --border:        #2E3348    (边框)

  同时提供亮色主题方案。

字体：
  - 界面文字：Inter / -apple-system / Microsoft YaHei UI
  - 数据/数字：JetBrains Mono / Consolas（等宽字体）
  - 标题：同界面字体，加粗

组件规范：
  - 圆角：小组件 6px，卡片 8px，模态框 12px
  - 阴影：暗色主题用边框代替阴影
  - 间距：基础单位 4px，常用 8/12/16/24/32
  - 过渡动画：所有状态变化 150-200ms ease

交互规范：
  - 所有可点击元素有明确 hover 状态（颜色变化 + cursor:pointer）
  - 拖拽时：拖拽物半透明 + 放置区描边高亮
  - 加载态：关键区域用 Skeleton 占位（不用 spinner）
  - 操作反馈：成功/错误 Toast 通知（右上角，3秒自动消失）
  - 确认危险操作（删除）：红色确认弹窗
  - 长操作：进度条 + 预估剩余时间

图标：使用 Lucide Icons 或 Phosphor Icons（一套统一风格）

响应式：
  - 最小宽度 1024px（数据分析工具不需要支持手机）
  - 侧边栏可折叠为图标模式
  - 面板可拖拽调整宽度
```

---

## Prompt 10：技术架构

```
DataViz Studio 技术架构设计。

项目结构：
dataviz-studio/
├── app.py                     # 应用入口 + Dash app 初始化
├── cli.py                     # 命令行入口 (dataviz-studio 命令)
├── config.py                  # 全局配置
├── requirements.txt
├── setup.py / pyproject.toml  # 包配置
│
├── core/
│   ├── data_manager.py        # 全局数据管理（多 DataFrame，undo/redo）
│   ├── state_manager.py       # 应用状态管理（dcc.Store 封装）
│   ├── project_io.py          # 项目文件(.dvs)读写
│   ├── code_generator.py      # 操作 → pandas 代码生成
│   └── event_bus.py           # 组件间事件通信
│
├── pages/                     # Dash Pages（每个功能模块一个页面）
│   ├── welcome.py             # 欢迎页
│   ├── data_hub.py            # 数据中心
│   ├── data_canvas.py         # 数据画布
│   ├── data_workshop.py       # 数据工坊
│   ├── chart_studio.py        # 图表工作室
│   ├── dashboard.py           # 仪表盘构建器
│   ├── statistics_lab.py      # 统计实验室
│   └── advanced_tools.py      # 高级工具
│
├── components/                # 可复用 UI 组件
│   ├── data_table.py          # AG Grid 封装
│   ├── chart_builder.py       # 图表构建器组件
│   ├── field_panel.py         # 字段拖拽面板
│   ├── filter_builder.py      # 筛选条件构建器
│   ├── kpi_card.py            # KPI 指标卡
│   ├── pipeline_view.py       # 操作流水线视图
│   ├── toast.py               # Toast 通知
│   └── theme_switcher.py      # 主题切换器
│
├── services/                  # 业务逻辑层（不涉及 UI）
│   ├── data_loader.py         # 数据加载（文件/数据库/URL）
│   ├── data_cleaner.py        # 数据清洗操作
│   ├── chart_service.py       # 图表生成逻辑
│   ├── stats_service.py       # 统计分析逻辑
│   ├── export_service.py      # 导出逻辑（PNG/PDF/HTML）
│   └── nlp_query.py           # 自然语言查询（可选）
│
├── assets/                    # Dash 静态资源（自动加载）
│   ├── css/
│   │   ├── base.css           # 基础样式 + 变量
│   │   ├── components.css     # 组件样式
│   │   ├── dark-theme.css     # 暗色主题
│   │   └── light-theme.css    # 亮色主题
│   ├── icons/                 # SVG 图标
│   └── images/                # Logo 等图片
│
└── utils/
    ├── helpers.py             # 通用工具函数
    ├── validators.py          # 数据校验
    └── i18n.py                # 国际化（中英文切换）

技术要点：

1. Dash 回调架构：
   - 使用 Pattern-Matching Callbacks 处理动态组件
   - 用 Dash Long Callbacks + Celery 处理耗时操作
   - 用 clientside_callback 处理纯前端交互（拖拽等）

2. 状态管理：
   - dcc.Store（session 级别）存储当前状态
   - 服务端缓存大数据（Flask-Caching / diskcache）
   - 操作历史栈（undo/redo 实现）

3. 性能优化：
   - 大数据集：服务端分页 + AG Grid 虚拟滚动
   - 图表：数据点过多时自动降采样
   - 异步加载：非关键数据延迟加载

4. 拖拽实现：
   - 使用 dash-draggable 或自定义 JS 组件
   - 字段拖拽到图表区域是核心交互，必须流畅

5. 可扩展性：
   - 每个 page 模块独立，共享 core 和 services
   - 新增功能 = 新增一个 page + 注册到导航
```

---

## Prompt 11：开发阶段与质量

```
分阶段交付，每阶段必须可独立运行和演示。

Phase 1 — 可用骨架（2周）：
  ✅ 应用框架 + 暗色主题 + 导航路由
  ✅ 欢迎页（拖拽上传 + 示例数据）
  ✅ 数据加载（CSV / Excel / JSON）
  ✅ 数据表格预览（AG Grid）
  ✅ 数据概览卡片
  → 目标：能加载数据、看到表格、感受到产品质感

Phase 2 — 核心体验（3周）：
  ✅ 图表工作室（拖拽式，10+ 图表类型）
  ✅ 智能图表推荐
  ✅ 样式配置面板
  ✅ 图表导出
  → 目标：能像 Tableau 一样拖拽创建图表

Phase 3 — 分析能力（2周）：
  ✅ 数据清洗面板 + 操作流水线
  ✅ 描述性统计 + 相关性分析
  ✅ 筛选构建器
  ✅ 代码生成功能
  → 目标：完整的数据分析流程

Phase 4 — 看板与高级（3周）：
  ✅ 仪表盘构建器 + 交叉筛选
  ✅ KPI 指标卡
  ✅ 假设检验向导
  ✅ 数据透视表
  → 目标：可与 Power BI 对比演示

Phase 5 — 差异化（持续）：
  ✅ 数据故事
  ✅ 时间序列分析
  ✅ 数据对比
  ✅ 项目保存/加载
  ✅ 导出为 Jupyter Notebook
  → 目标：超越 Power BI 免费版

代码质量要求：
- 完整的类型标注（type hints）
- 每个公开函数/类有 docstring
- 模块间解耦，可独立测试
- CSS 使用 BEM 命名或 CSS Modules 思路
- 遵循 PEP 8
```

---

## 使用说明

> Prompt 0 是基础上下文，**每次对话必须首先提供**。
> 然后根据当前要实现的模块，附加对应的 Prompt。

| 场景 | 使用方式 |
|------|---------|
| 新开项目 | Prompt 0 + Prompt 1（骨架）+ Prompt 9（设计系统） |
| 实现某模块 | Prompt 0 + 对应 Prompt |
| 讨论架构 | Prompt 0 + Prompt 10 |
| 规划进度 | Prompt 0 + Prompt 11 |
| 做差异化功能 | Prompt 0 + Prompt 8（开放式，给 AI 发挥空间） |

> [!TIP]
> Prompt 8（高级与独创功能）故意写得开放，鼓励 AI 根据技术趋势自由发挥。
> 你可以在对话中追加约束或方向来引导。

> [!IMPORTANT]
> 这是第二套提示词（独立 Web 应用版），与第一套（JupyterLab 内嵌版）定位不同。
> 两套可以作为两个独立项目并行推进，也可以只选一个先做。

# 🧪 DataViz Studio — AI 项目构建提示词

> **给 AI 用的提示词集**：用于在 JupyterLab 内构建一个 Power BI / Tableau 级别的零代码数据分析 GUI 工具。

---

## Prompt 0：项目总纲（每次对话必须首先提供）

```
你是一个资深 Python 数据工具开发者。你正在构建 DataViz Studio —— 一个运行在
JupyterLab Notebook Cell 内的零代码数据分析 GUI 平台。

核心理念：
- 用户在 Notebook Cell 中输入一行 `Studio(df)` 即可弹出完整 GUI
- GUI 以 ipywidgets 为基础，在 Cell 输出区渲染为一个全功能交互界面
- 用户通过点击、拖拽、选择完成所有操作，全程不写代码
- 提供 Power BI / Tableau 级别的交互体验
- 底层整合 PygWalker、D-Tale、Mito、ydata-profiling 等成熟工具作为可插拔模块
- 同时包含这些工具没有覆盖的原创功能模块
- 所有操作可选择性地同步生成对应 pandas 代码到下方 Cell，供用户学习和复现

技术约束：
- 运行环境：Anaconda + JupyterLab 4.x
- GUI 框架：ipywidgets + ipydatagrid + anywidget（如需自定义组件）
- 可视化：plotly（交互式图表首选）、matplotlib（静态图表）、echarts（可选）
- 数据处理：pandas, numpy, scipy
- 安装方式：pip install dataviz-studio 或 conda install dataviz-studio
- 入口极简：from dataviz_studio import Studio; Studio(df)

设计原则：
- 新手友好：不需要任何编程知识即可使用
- 模块化：每个功能是独立模块，可单独加载
- 渐进式：简单操作一步到位，高级功能按需展开
- 美观：深色主题优先，现代 UI 设计，流畅动画
```

---

## Prompt 1：应用骨架与入口

```
构建 DataViz Studio 的应用骨架。要求：

入口设计：
  from dataviz_studio import Studio
  s = Studio(df)          # 传入 DataFrame，弹出完整 GUI
  s = Studio("data.csv")  # 也可以直接传文件路径
  s = Studio()            # 空启动，显示欢迎页 + 文件导入区

GUI 主框架（在 Cell 输出区渲染）：
  ┌─ 顶栏 ─────────────────────────────────────┐
  │ Logo  [模块标签页...]     [主题] [设置] [帮助]│
  ├──────┬──────────────────────┬────────────────┤
  │ 左侧 │    中央工作区         │ 右侧配置面板    │
  │ 数据 │  （内容随标签页切换） │ （上下文相关）   │
  │ 面板 │                     │                │
  ├──────┴──────────────────────┴────────────────┤
  │ 底栏：数据集信息 | 操作日志 | 代码生成开关      │
  └─────────────────────────────────────────────┘

关键要求：
- 整体高度自适应但有最小高度（600px），可拖拽调整
- 左右面板可折叠
- 标签页切换时平滑过渡
- 响应式布局，适配不同 JupyterLab 窗口宽度
- 底栏"代码生成"开关打开后，每次操作自动在 Notebook 下方
  插入对应的 pandas 代码 Cell（用 IPython API）

请同时设计插件注册机制：每个功能模块以 Plugin 基类注册，
主框架自动发现并加载为标签页。这样未来添加新功能只需写一个
新模块类即可。
```

---

## Prompt 2：数据加载与管理

```
构建数据加载与管理模块，这是所有分析的起点。

数据输入方式（支持以下所有方式，用户通过 GUI 面板选择）：
- 本地文件：CSV / Excel / JSON / Parquet / Feather / HDF5 / STATA / SAS
  → 文件选择器 + 编码自动检测 + 分隔符自动推断
- 拖拽上传：直接拖文件到界面中
- 数据库连接：SQLite / MySQL / PostgreSQL
  → 可视化连接配置表单 + SQL 查询编辑器（带语法高亮）
- URL 导入：输入链接直接下载 CSV/JSON
- 剪贴板：一键从剪贴板粘贴表格数据
- 示例数据集：内置 seaborn/sklearn 经典数据集一键加载

数据管理面板（左侧常驻）：
- 当前加载的所有 DataFrame 列表
- 每个 DataFrame 显示：名称、行列数、内存大小
- 点击切换活跃数据集
- 支持多数据集同时管理，可在模块间切换使用
- 支持数据集合并/拼接操作（GUI 化的 merge/concat）

大文件策略：
- > 50MB 文件自动提示采样预览选项
- 支持分块加载 + 进度条
- 虚拟滚动表格（只渲染可见行）
```

---

## Prompt 3：数据预览与概览

```
构建数据预览与概览模块，让用户一眼理解数据全貌。

数据表格视图：
- 高性能可滚动表格（基于 ipydatagrid 或类似方案）
- 列头显示：列名 + 数据类型图标 + 排序按钮
- 单元格条件格式化（数值梯度着色、空值高亮标红）
- 列宽可拖拽调整
- 右键列头菜单：排序/筛选/隐藏/重命名/类型转换
- 冻结前 N 列 / 前 N 行

数据概览仪表盘（展示在表格上方或作为子标签页）：
- 核心指标卡片组：总行数 / 总列数 / 缺失值总数 / 重复行数 / 内存占用
- 列类型分布饼图（数值、分类、日期、布尔...）
- 缺失值矩阵可视化（哪些列在哪些行有缺失，整体模式）
- 每列迷你概要：
  - 数值列：分布直方图 + 均值/中位数/标准差
  - 分类列：类别频率条形图 + 唯一值数
  - 日期列：时间范围 + 时间线预览
- 数据质量评分（0-100分，综合考虑缺失值、重复、异常值）

可以整合 ydata-profiling 作为"一键生成完整报告"的深度选项，
但轻量概览应该是自建的、即时的、不需要等待的。
```

---

## Prompt 4：数据清洗与转换

```
构建数据清洗与转换模块，全程 GUI 操作。

界面形式：左侧操作菜单 + 中间预览 + 右侧操作日志流水线

提供以下清洗操作（全部通过点击/选择完成）：

列操作：
- 重命名列（点击列头直接编辑）
- 删除列（勾选 + 删除按钮）
- 调整列顺序（拖拽排序）
- 拆分列（指定分隔符）
- 合并列（选多列 + 分隔符 + 新列名）

缺失值处理：
- 可视化展示哪些列有多少缺失值（条形图）
- 处理策略选择器：
  ├── 删除含缺失值的行
  ├── 填充：均值 / 中位数 / 众数 / 固定值 / 前向填充 / 后向填充
  └── 用机器学习插值（KNN填充）
- 按列独立选择策略

数据类型转换：
- 下拉选择目标类型 + 实时预览转换结果
- 智能建议（检测到"2024-01-01"的字符串列自动建议转为日期）

筛选与排序：
- 可视化筛选构建器（条件卡片式，拖拽组合 AND/OR）
- 多列排序器
- 去重（选择保留首个/末个/全删）

文本清洗（针对字符串列）：
- 去空格 / 大小写转换 / 正则替换
- 提取模式（如从地址中提取城市）

新增计算列：
- 公式编辑器（支持 pandas 表达式，带自动补全和语法提示）
- 常用计算模板（同比/环比/排名/分箱...）

操作流水线（关键特性）：
- 每次操作记录为一个步骤卡片
- 支持拖拽排序、删除、禁用单个步骤
- 一键撤销 / 重做
- 应用前可预览效果
- 整条流水线可导出为 Python 函数
```

---

## Prompt 5：可视化图表引擎（核心模块）

```
构建可视化图表引擎，这是 DataViz Studio 最重要的模块，
要提供媲美 Tableau / Power BI 的交互式图表创建体验。

交互方式：
- 左侧字段面板：列出所有列，按类型分组（度量/维度自动识别）
- 拖拽字段到：X轴 / Y轴 / 颜色 / 大小 / 分面 / 提示信息
- 也可以通过下拉选择而非拖拽（移动端友好）
- 拖入字段后图表实时更新，延迟 < 500ms

图表类型（按场景分组展示，点击图标切换）：
- 比较类：柱状图、分组/堆叠柱状图、条形图、雷达图
- 趋势类：折线图、面积图、阶梯图
- 分布类：直方图、箱线图、小提琴图、密度图、QQ图
- 关系类：散点图、气泡图、热力图、相关矩阵
- 占比类：饼图、环形图、旭日图、树状图
- 高级类：桑基图、平行坐标图、瀑布图、漏斗图
- 地理类：地图（如果数据包含经纬度或地名）

智能推荐：
- 根据所选字段的数据类型自动推荐最适合的图表类型
- 不合适的图表类型灰显并提示原因
- 推荐配色方案

样式配置面板（右侧）：
- 主题预设：10+ 套（商务/学术/科技/自然/暗色系...）
- 调色板选择器（提供经典调色板 + 自定义色值）
- 字体、标题、副标题、轴标签全部可编辑
- 图例位置/方向调整
- 网格线/背景色开关
- 数据标签显示配置
- 动画效果开关

高级功能：
- 子图/网格布局：创建 M×N 图表网格，每格独立配置
- 多图联动：hover/缩放/筛选同步
- 图表注释：点击图表添加箭头/文本/高亮区域
- 趋势线：线性/多项式/移动平均，一键叠加
- 参考线：添加均值线/中位数线/自定义阈值线
- 图表动画/时间轴播放（如果有时间维度）

导出：
- PNG / SVG / PDF（静态高清）
- HTML（保留交互功能的独立文件）
- 直接复制到剪贴板
```

---

## Prompt 6：统计分析模块

```
构建统计分析模块，让非统计专业的人也能轻松做分析。

界面理念：不展示数学公式，用自然语言和可视化解释每个结果。

描述性统计：
- 选择列 → 一键显示完整统计摘要
- 结果用卡片形式美化展示，不是干巴巴的表格
- 包含分布图配合解读（"此列呈右偏分布，均值显著大于中位数"）

相关性分析：
- 交互式相关矩阵热力图
- 点击任意两列交叉点 → 弹出散点图 + 拟合线
- 自动高亮强相关 / 强负相关的变量对
- 提供解读提示

分组分析器：
- 选择分组列 → 选择聚合函数（计数/求和/均值/中位数...）
- 即时显示结果表格 + 可视化
- 支持多级分组

假设检验工具箱（引导式）：
- 用户不需要知道"t检验"这个名字
- 而是回答问题："你想比较什么？"
  → "两组数据的均值是否有差异" → 自动选择 t 检验
  → "多组数据是否有差异" → 自动选择 ANOVA
  → "两个分类变量是否相关" → 自动选择卡方检验
- 结果用自然语言解释："两组数据存在显著差异（p=0.003 < 0.05）"

异常值检测：
- 多种方法可选（IQR / Z-Score / Isolation Forest）
- 在图表上高亮标注异常点
- 一键选择处理方式（标记/删除/替换为边界值）
```

---

## Prompt 7：独创功能模块（超越现有工具）

```
以下功能是 PygWalker、D-Tale 等现有工具不具备的差异化功能，
请创造性地设计和实现。以下是方向性描述，不是严格规格，
请根据技术可行性和用户体验自行发挥最佳方案。

1. 数据故事（Data Story Builder）
   把多个图表 + 文本注释 + 分析结论编排成一个可滚动的"数据故事"，
   类似 Scrollytelling。可导出为独立 HTML 页面或 PDF 报告。
   想象一个分析师做完分析后，给老板看的不是代码，而是一篇图文并茂的报告。

2. 时间序列专区
   针对时间数据提供专门的分析套件：
   - 趋势分解（趋势 + 季节性 + 残差）
   - 移动平均 / 指数平滑 / 自相关图
   - 简单预测（基于历史趋势向前推算）
   - 多个时间序列对比面板

3. 智能数据对比
   选择两个 DataFrame 或同一 DataFrame 的两个子集：
   - 自动对比各列统计指标差异
   - 并排分布图对比
   - 差异热力图
   用例：对比上月 vs 本月、A组 vs B组、清洗前 vs 清洗后

4. 特征工程助手
   面向机器学习准备阶段：
   - 自动检测并建议转换（Log变换、标准化、独热编码...）
   - 特征重要性评估（用随机森林快速估算）
   - 特征交互建议
   - 一键生成 scikit-learn Pipeline 代码

5. 自然语言查询（如技术可行）
   输入框输入自然语言，自动转为 pandas 操作并执行：
   "按城市分组统计总销售额，从高到低排列"
   → df.groupby('city')['sales'].sum().sort_values(ascending=False)
   这个功能可以集成大语言模型 API，也可以做基于规则的简单版本。

6. 协作与分享
   - 当前分析状态可到出为 .dvs 项目文件（JSON 格式）
   - 导出完整 Jupyter Notebook（包含所有操作对应的代码）
   - 分享为静态 HTML Dashboard

请根据以上方向自由发挥，优先实现技术可行性高、用户价值大的功能。
不必一次全部实现，但架构设计上要预留扩展空间。
```

---

## Prompt 8：UI/UX 设计系统

```
为 DataViz Studio 设计一套完整的 UI 设计系统，
在 ipywidgets 的约束下做到最好的视觉效果。

设计目标：专业、现代、不像一个粗糙的 Notebook 插件，
而是像一个嵌入在 Notebook 中的精致应用。

配色（暗色主题，默认）：
- 与 JupyterLab 暗色主题协调
- 遵循信息设计原则：数据用饱和色，UI chrome 用低饱和色
- 不硬编码色值，设计一套 CSS 变量 / 主题 token

交互细节（这些决定了"专业感"）：
- 所有可点击元素有 hover 状态
- 拖拽时有视觉反馈（影子 + 放置区高亮）
- 长时间操作有进度提示（进度条或 skeleton 加载态）
- 操作成功/失败有 toast 通知
- 面板过渡要平滑，避免生硬切换
- 图表加载用 skeleton 占位，不要空白闪烁

图标系统：
- 使用一套统一的图标库（推荐 phosphor-icons 或内嵌 SVG）
- 每个功能模块有辨识度高的图标

ipywidgets 的样式突破：
- 利用 CSS 注入覆盖默认样式
- anywidget 可用于创建完全自定义的组件
- 研究在 ipywidgets 中嵌入 HTML/JS 的最佳实践以突破样式限制

请把设计系统实现为一个可复用的样式模块，
所有功能模块统一引用，确保全局一致性。
```

---

## Prompt 9：插件架构与技术实现

```
DataViz Studio 的技术架构设计，重点是可扩展性。

项目结构：
dataviz_studio/
├── __init__.py              # 暴露 Studio 入口
├── core/
│   ├── app.py               # 主应用框架、标签页管理
│   ├── plugin_base.py       # Plugin 基类定义
│   ├── plugin_registry.py   # 插件注册与发现
│   ├── data_manager.py      # 全局数据管理器（多 DataFrame）
│   ├── event_bus.py         # 模块间通信（观察者模式）
│   ├── code_generator.py    # pandas 代码生成引擎
│   └── theme.py             # 主题与样式管理
├── plugins/
│   ├── data_loader/         # 数据加载模块
│   ├── data_preview/        # 数据预览与概览
│   ├── data_cleaning/       # 数据清洗与转换
│   ├── chart_engine/        # 可视化图表引擎
│   ├── statistics/          # 统计分析
│   ├── pygwalker_bridge/    # PygWalker 集成桥接
│   ├── dtale_bridge/        # D-Tale 集成桥接
│   ├── mito_bridge/         # Mito 集成桥接
│   ├── profiling_bridge/    # ydata-profiling 集成
│   ├── time_series/         # 时间序列分析
│   ├── data_story/          # 数据故事构建
│   ├── feature_eng/         # 特征工程助手
│   └── nl_query/            # 自然语言查询
├── ui/
│   ├── components/          # 可复用 UI 组件库
│   ├── styles/              # CSS 样式文件
│   └── icons/               # 图标资源
└── utils/
    ├── io.py                # 文件读写工具
    ├── validators.py        # 数据校验
    └── helpers.py           # 通用辅助函数

Plugin 基类设计要点：
class StudioPlugin:
    name: str                   # 显示名称
    icon: str                   # 图标
    description: str            # 功能描述
    priority: int               # 标签页排序权重
    def activate(self, data_manager) → Widget:   # 返回标签页内容
    def on_data_changed(self, df):               # 响应数据集变化
    def get_generated_code(self) → str:          # 返回操作对应的代码

关键技术决策：
- 模块间通过 EventBus 通信，解耦
- 数据变更走统一的 DataManager，支持 undo/redo 栈
- 第三方工具桥接：用 iframe 或 ipywidgets 的 Output 组件隔离
- 代码生成：每个操作注册对应的代码模板，CodeGenerator 汇总输出
- 主题：一套 CSS 变量，通过 display(HTML(css)) 注入 Cell

请确保架构足够灵活，用户或社区可以开发第三方插件并注册进来。
```

---

## Prompt 10：开发节奏与质量要求

```
分阶段开发，每个阶段必须可运行、可演示。

Phase 1 — 骨架可用（MVP）：
  - 应用框架 + 插件系统 + 主题系统
  - 数据加载（CSV / Excel）
  - 数据表格预览
  - 基础图表（5 种以上）
  → 目标：Studio(df) 能弹出界面，能看数据，能出图表

Phase 2 — 核心完善：
  - 数据清洗面板 + 操作流水线
  - 完整图表引擎（15+ 图表类型）
  - 描述性统计 + 相关性分析
  - 代码生成功能
  → 目标：可以完成一个完整的数据分析流程

Phase 3 — 集成与创新：
  - PygWalker / D-Tale / Mito / ydata-profiling 桥接
  - 时间序列模块
  - 数据对比
  - 特征工程助手
  → 目标：功能超越任何单一现有工具

Phase 4 — 体验升级：
  - 数据故事构建器
  - 自然语言查询
  - 性能优化
  - 打包发布到 PyPI / conda-forge

代码质量：
- 类型标注（type hints）
- docstring 文档
- 核心模块有单元测试
- 代码风格遵循 PEP 8
```

---

## 使用说明

> 以上 Prompt 0-10 构成完整的项目提示词体系。使用方式：

| 场景 | 使用哪些 Prompt |
|------|----------------|
| 开始新项目 | Prompt 0（总纲），必须作为系统上下文 |
| 实现某个模块 | Prompt 0 + 对应模块 Prompt |
| 讨论技术方案 | Prompt 0 + Prompt 9（架构） |
| 设计界面 | Prompt 0 + Prompt 8（UI/UX） |
| 规划进度 | Prompt 0 + Prompt 10（开发节奏） |

> [!TIP]
> Prompt 7（独创功能）故意写得比较开放，给 AI 自由发挥空间。
> 你可以在对话中追加具体需求来引导方向。

# DataViz Studio 功能增强计划
## 基于 D-Tale 和 PyGWalker 的全面功能集成

**创建日期**: 2026-02-26  
**目标**: 集成 D-Tale 和 PyGWalker 的所有可实现功能

---

## 📊 功能分类总览

### 从 D-Tale 学习的功能（60+ 功能）
1. **数据清洗** (20+ 功能)
2. **列操作** (15+ 功能)
3. **数据转换** (10+ 功能)
4. **统计分析** (10+ 功能)
5. **数据可视化** (5+ 功能)

### 从 PyGWalker 学习的功能（30+ 功能）
1. **智能字段识别** (5+ 功能)
2. **图表推荐** (10+ 功能)
3. **数据聚合** (8+ 功能)
4. **交互设计** (7+ 功能)

---

## 🎯 Phase 1: 核心数据清洗功能（优先级：高）

### 1.1 列操作增强

#### ✅ 已实现
- 删除列
- 重命名列
- 填充缺失值（均值、中位数、众数、前向/后向填充）
- 类型转换

#### 📋 待实现（从 D-Tale）

**1. 拆分列 (Split Column)**
```python
# 功能：按分隔符拆分列
# 示例：将 "姓名" 列拆分为 "姓" 和 "名"
# 参数：
- 源列
- 分隔符（逗号、空格、自定义）
- 拆分数量（2列、3列、全部）
- 新列名前缀
```

**2. 合并列 (Concatenate Column)**
```python
# 功能：合并多列为一列
# 示例：合并 "姓" 和 "名" 为 "全名"
# 参数：
- 源列列表
- 分隔符
- 新列名
```

**3. 提取子字符串 (Substring)**
```python
# 功能：从字符串列提取部分内容
# 参数：
- 源列
- 起始位置
- 结束位置（或长度）
- 新列名
```

**4. 查找替换 (Find & Replace)**
```python
# 功能：查找并替换列中的值
# 参数：
- 源列
- 查找内容（支持正则表达式）
- 替换内容
- 是否区分大小写
```

**5. 去除空格 (Strip Whitespace)**
```python
# 功能：去除字符串前后空格
# 选项：
- 去除前导空格
- 去除尾随空格
- 去除所有空格
- 替换多个空格为单个
```

**6. 大小写转换 (Case Conversion)**
```python
# 选项：
- 全部大写 (UPPER)
- 全部小写 (lower)
- 首字母大写 (Title Case)
- 句首大写 (Sentence case)
```

---

### 1.2 数值处理

#### 📋 待实现（从 D-Tale）

**1. 分箱 (Binning)**
```python
# 功能：将连续数值分组
# 方法：
- 等宽分箱（Equal Width）
- 等频分箱（Equal Frequency）
- 自定义边界
# 参数：
- 源列
- 分箱数量
- 标签（可选）
```

**2. 标准化 (Standardization)**
```python
# 功能：Z-score 标准化
# 公式：(x - mean) / std
# 参数：
- 源列
- 新列名
```

**3. 归一化 (Normalization)**
```python
# 功能：Min-Max 归一化
# 公式：(x - min) / (max - min)
# 参数：
- 源列
- 目标范围（默认 0-1）
- 新列名
```

**4. Winsorize（异常值处理）**
```python
# 功能：限制极端值
# 参数：
- 源列
- 下限百分位（如 5%）
- 上限百分位（如 95%）
```

**5. 对数转换 (Log Transform)**
```python
# 选项：
- log (自然对数)
- log10
- log2
```

**6. 差分 (Difference)**
```python
# 功能：计算相邻行的差值
# 用途：时间序列分析
# 参数：
- 源列
- 差分阶数（1阶、2阶）
```

---

### 1.3 计算列增强

#### ✅ 已实现
- 基础计算列（加减乘除）

#### 📋 待实现（从 D-Tale）

**1. 数值运算**
```python
# 已有：+, -, *, /
# 新增：
- 取模 (%)
- 幂运算 (**)
- 绝对值 (abs)
- 四舍五入 (round)
- 向上取整 (ceil)
- 向下取整 (floor)
```

**2. 聚合函数**
```python
# 按组计算：
- sum (求和)
- mean (平均值)
- median (中位数)
- min/max (最小/最大值)
- count (计数)
- std (标准差)
```

**3. 窗口函数**
```python
# 滚动窗口：
- 滚动平均 (rolling mean)
- 滚动求和 (rolling sum)
- 滚动最大/最小值
- 指数平滑 (exponential smoothing)
```

**4. 累积函数**
```python
- cumsum (累积和)
- cumprod (累积积)
- cummax/cummin (累积最大/最小)
```

**5. 时间序列**
```python
- shift (移位)
- lag (滞后)
- lead (超前)
- 时间差 (time delta)
```

---

### 1.4 缺失值处理增强

#### ✅ 已实现
- 查看缺失值统计
- 填充缺失值（均值、中位数、众数、前向/后向填充、固定值）
- 删除缺失行

#### 📋 待实现（从 D-Tale）

**1. 插值填充 (Interpolation)**
```python
# 方法：
- 线性插值 (linear)
- 多项式插值 (polynomial)
- 样条插值 (spline)
- 时间插值 (time)
```

**2. 迭代填充 (Iterative Imputer)**
```python
# 使用机器学习模型预测缺失值
# 基于其他列的值
```

**3. KNN 填充**
```python
# 使用 K 近邻算法填充
# 参数：
- K 值
- 距离度量
```

---

### 1.5 重复值处理

#### 📋 待实现（从 D-Tale）

**1. 查找重复值**
```python
# 功能：
- 显示重复行
- 按列查找重复
- 统计重复次数
```

**2. 删除重复值**
```python
# 选项：
- 保留第一个
- 保留最后一个
- 删除全部
- 按特定列判断
```

**3. 标记重复值**
```python
# 创建新列标记是否重复
```

---

## 🎨 Phase 2: 图表功能增强（优先级：高）

### 2.1 智能字段识别（从 PyGWalker）

#### 📋 待实现

**1. 自动类型推断**
```python
def infer_field_type(series):
    """
    推断字段类型：
    - quantitative (数值型 - 度量)
    - temporal (时间型)
    - nominal (分类型 - 维度)
    - ordinal (有序分类)
    """
```

**2. 字段元数据**
```python
# 为每个字段生成元数据：
{
    "name": "销售额",
    "type": "quantitative",
    "role": "measure",  # measure 或 dimension
    "dataType": "number",
    "unique_count": 1000,
    "null_count": 5,
    "min": 0,
    "max": 10000
}
```

**3. 智能字段面板**
```python
# UI 改进：
- 维度区域（Dimensions）
- 度量区域（Measures）
- 字段图标（📊 数值、📅 日期、🔤 文本）
- 字段统计信息
```

---

### 2.2 图表类型扩展

#### ✅ 已实现
- Plotly: 散点图、折线图、柱状图、直方图、箱线图、小提琴图、3D散点图、饼图、旭日图、矩形树图、漏斗图、密度热力图
- Seaborn: 散点图、折线图、柱状图、直方图、箱线图、小提琴图、热力图

#### 📋 待实现（从 D-Tale 和 PyGWalker）

**Plotly 新增图表**:
```python
1. 面积图 (Area Chart)
2. 瀑布图 (Waterfall Chart)
3. 甘特图 (Gantt Chart)
4. 桑基图 (Sankey Diagram)
5. 雷达图 (Radar Chart)
6. 极坐标图 (Polar Chart)
7. 地图 (Choropleth Map) - 可选
8. 3D 曲面图 (3D Surface)
9. 等高线图 (Contour Plot)
10. 平行坐标图 (Parallel Coordinates)
```

**Seaborn 新增图表**:
```python
1. 配对图 (Pair Plot)
2. 联合图 (Joint Plot)
3. 回归图 (Regression Plot)
4. 分类图 (Cat Plot)
5. 计数图 (Count Plot)
6. 点图 (Point Plot)
7. 条形图 (Bar Plot)
8. 簇状热力图 (Clustermap)
```

---

### 2.3 图表参数增强

#### 📋 待实现

**1. 动态参数面板**
```python
# 根据图表类型显示相关参数
# 示例：饼图
{
    "required": ["names", "values"],
    "optional": ["hole", "pull"]
}

# 示例：散点图
{
    "required": ["x", "y"],
    "optional": ["color", "size", "hover_data", "trendline"]
}
```

**2. 参数分组**
```python
# 基础参数
- X 轴、Y 轴、颜色、大小

# 高级参数
- 分面（facet）
- 动画（animation）
- 趋势线（trendline）
- 边际图（marginal）

# 样式参数
- 颜色方案
- 标题和标签
- 图例位置
- 坐标轴范围
```

**3. 参数验证**
```python
# 智能验证：
- 数值字段只能用于需要数值的参数
- 分类字段只能用于分组参数
- 时间字段自动格式化
```

---

### 2.4 图表交互增强

#### 📋 待实现（从 D-Tale）

**1. 图表联动**
```python
# 点击图表元素，筛选数据表
# 示例：点击柱状图的某个柱子，表格只显示该类别数据
```

**2. 图表导出**
```python
# 导出格式：
- PNG (静态图片)
- SVG (矢量图)
- HTML (交互式)
- PDF (报告)
```

**3. 图表模板**
```python
# 保存常用图表配置
# 快速应用到新数据
```

---

## 📈 Phase 3: 统计分析功能（优先级：中）

### 3.1 描述性统计

#### 📋 待实现（从 D-Tale）

**1. 数据概览**
```python
# 显示：
- 行数、列数
- 内存使用
- 数据类型分布
- 缺失值统计
```

**2. 列统计**
```python
# 数值列：
- 均值、中位数、众数
- 标准差、方差
- 最小值、最大值
- 四分位数
- 偏度、峰度

# 分类列：
- 唯一值数量
- 最频繁值
- 频率分布
```

**3. 相关性分析**
```python
# 功能：
- 相关系数矩阵
- 相关性热力图
- 散点矩阵
- 时间序列相关性
```

---

### 3.2 数据分布

#### 📋 待实现（从 D-Tale）

**1. 直方图分析**
```python
# 参数：
- 分箱数量
- 密度曲线
- 正态分布拟合
```

**2. QQ 图**
```python
# 检验数据是否符合正态分布
```

**3. 箱线图分析**
```python
# 显示：
- 异常值
- 四分位数
- 中位数
```

---

### 3.3 假设检验

#### 📋 待实现（从 D-Tale）

**1. T 检验**
```python
# 单样本 t 检验
# 双样本 t 检验
# 配对 t 检验
```

**2. 方差分析 (ANOVA)**
```python
# 单因素方差分析
# 多因素方差分析
```

**3. 卡方检验**
```python
# 独立性检验
# 拟合优度检验
```

---

## 🔄 Phase 4: 数据转换功能（优先级：中）

### 4.1 数据重塑

#### 📋 待实现（从 D-Tale）

**1. 透视表 (Pivot Table)**
```python
# 参数：
- 行索引
- 列索引
- 值字段
- 聚合函数
```

**2. 逆透视 (Melt)**
```python
# 将宽格式转为长格式
# 参数：
- ID 列
- 值列
- 变量名
- 值名
```

**3. 堆叠/拆分 (Stack/Unstack)**
```python
# 多级索引操作
```

---

### 4.2 数据合并

#### 📋 待实现（从 D-Tale）

**1. 连接 (Join)**
```python
# 类型：
- 内连接 (inner)
- 左连接 (left)
- 右连接 (right)
- 外连接 (outer)

# 参数：
- 连接键
- 后缀处理
```

**2. 合并 (Merge)**
```python
# 基于列值合并
```

**3. 拼接 (Concatenate)**
```python
# 垂直拼接（行）
# 水平拼接（列）
```

---

### 4.3 数据采样

#### 📋 待实现（从 D-Tale）

**1. 随机采样**
```python
# 参数：
- 采样数量或比例
- 是否放回
- 随机种子
```

**2. 分层采样**
```python
# 按类别比例采样
```

**3. 时间采样**
```python
# 重采样时间序列数据
# 上采样/下采样
```

---

## 🎯 Phase 5: 高级功能（优先级：低）

### 5.1 数据导入/导出增强

#### 📋 待实现

**1. 多格式支持**
```python
# 导入：
- CSV, Excel, JSON
- Parquet, Feather
- SQL 数据库
- API 接口

# 导出：
- 所有导入格式
- HTML 报告
- LaTeX 表格
```

**2. 数据预览**
```python
# 导入前预览数据
# 选择列
# 设置数据类型
```

---

### 5.2 自动化和脚本

#### 📋 待实现（从 D-Tale）

**1. 操作录制**
```python
# 记录所有操作
# 生成完整 Python 脚本
# 可重复执行
```

**2. 批处理**
```python
# 对多个文件执行相同操作
```

---

### 5.3 协作功能

#### 📋 待实现

**1. 分享**
```python
# 生成分享链接
# 导出为 HTML 报告
```

**2. 注释**
```python
# 为数据添加注释
# 标记重要发现
```

---

## 📊 实现优先级矩阵

### 高优先级（立即实现）
| 功能 | 难度 | 价值 | 预计时间 |
|------|------|------|----------|
| 拆分列 | 低 | 高 | 2小时 |
| 合并列 | 低 | 高 | 2小时 |
| 查找替换 | 中 | 高 | 3小时 |
| 分箱 | 中 | 高 | 3小时 |
| 标准化/归一化 | 低 | 高 | 2小时 |
| 智能字段识别 | 中 | 高 | 4小时 |
| 动态参数面板 | 中 | 高 | 4小时 |
| 新增图表类型 | 低 | 中 | 6小时 |

### 中优先级（后续实现）
| 功能 | 难度 | 价值 | 预计时间 |
|------|------|------|----------|
| 窗口函数 | 中 | 中 | 4小时 |
| 插值填充 | 中 | 中 | 3小时 |
| 透视表 | 高 | 中 | 6小时 |
| 相关性分析 | 中 | 中 | 4小时 |
| 数据合并 | 高 | 中 | 6小时 |

### 低优先级（可选）
| 功能 | 难度 | 价值 | 预计时间 |
|------|------|------|----------|
| 假设检验 | 高 | 低 | 8小时 |
| 地图可视化 | 高 | 低 | 10小时 |
| 机器学习填充 | 高 | 低 | 8小时 |

---

## 🚀 实施路线图

### Week 1: 核心数据清洗
- Day 1-2: 拆分列、合并列
- Day 3-4: 查找替换、字符串处理
- Day 5: 测试和文档

### Week 2: 数值处理
- Day 1-2: 分箱、标准化、归一化
- Day 3-4: 窗口函数、累积函数
- Day 5: 测试和文档

### Week 3: 图表增强
- Day 1-2: 智能字段识别
- Day 3-4: 动态参数面板
- Day 5: 新增图表类型

### Week 4: 统计分析
- Day 1-2: 描述性统计
- Day 3-4: 相关性分析
- Day 5: 数据分布分析

---

## 📝 技术实现笔记

### 关键代码参考

**D-Tale 列构建器模式**:
```python
# reference_projects/dtale/dtale/column_builders.py
class ColumnBuilder:
    def __init__(self, data_id, column_type, name, cfg):
        # 根据类型选择构建器
        if column_type == "numeric":
            self.builder = NumericColumnBuilder(name, cfg)
        elif column_type == "string":
            self.builder = StringColumnBuilder(name, cfg)
        # ...
```

**PyGWalker 字段推断**:
```python
# reference_projects/pygwalker/pygwalker/data_parsers/pandas_parser.py
def infer_semantic_type(series):
    if is_numeric_dtype(series):
        return "quantitative"
    elif is_datetime64_any_dtype(series):
        return "temporal"
    else:
        return "nominal"
```

---

## ✅ 成功标准

### 功能完整性
- [ ] 实现 80% 的 D-Tale 数据清洗功能
- [ ] 实现 60% 的 PyGWalker 智能功能
- [ ] 支持 30+ 种图表类型
- [ ] 提供 50+ 种数据操作

### 代码质量
- [ ] 所有功能有单元测试
- [ ] 代码覆盖率 > 80%
- [ ] 文档完整
- [ ] 性能优化

### 用户体验
- [ ] 操作直观易用
- [ ] 响应时间 < 1秒
- [ ] 错误提示清晰
- [ ] 支持撤销/重做

---

**文档版本**: 1.0  
**最后更新**: 2026-02-26  
**状态**: 规划中
